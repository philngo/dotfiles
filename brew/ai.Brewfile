cask "claude"
# claude-code: installed via native installer (auto-upgrades) — curl -fsSL https://claude.ai/install.sh | bash
cask "codex"
# tidewave: CLI installed via install.sh from GitHub releases (no brew formula/cask exists)
